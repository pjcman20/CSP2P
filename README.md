
  # Create Guidelines Document

  This is a code bundle for Create Guidelines Document. The original project is available at https://www.figma.com/design/m6Q9q0T6DSrolCQU11mFRH/Create-Guidelines-Document.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  